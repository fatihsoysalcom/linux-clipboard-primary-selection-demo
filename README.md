# linux-clipboard-primary-selection-demo
This Python Tkinter example demonstrates the distinct clipboard mechanisms in Linux (X11): PRIMARY and CLIPBOARD selections. Users can type and select text in the first box, then use buttons to explicitly copy/paste from CLIPBOARD (like Ctrl+C/Ctrl+V) or implicitly paste from PRIMARY (like middle-click after highlighting). It highlights how Linux m
